const temIngresso = true;
const censura = 16;
const idade = 16;

if (idade >= censura && temIngresso) {
    console.log(`Pode entrar no cinema!`);
}else {
    console.log(`Não pode entrar no cinema!`);
}